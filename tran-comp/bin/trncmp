#!/bin/bash

if [ -e target/uber-trncmp-1.2.0.jar ]; then
    java -Djava.net.preferIPv4Stack=true -jar target/uber-trncmp-1.2.0.jar $*
    exit $?
fi

if [ -e ephsim.jar ]; then
    java -Djava.net.preferIPv4Stack=true -jar trncmp.jar $*
    exit $?
fi

exit 0
